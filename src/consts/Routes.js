const ROUTES = {
    INTRO: '/',      // This is the root route
    MAIN: '/main',   // This is the route for the Main page
    DOWN: '/down',   // This is the route for the Down page
};

export default ROUTES;
